function Flexo(modelsURL) {
    var pos = new THREE.Vector3(-395, -546, -345);
    // F
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 10, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 10, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 20, pos.y, pos.z)
    );

    // L
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 40, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 40, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 40, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 40, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 40, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 50, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 60, pos.y, pos.z + 40)
    );

    // E
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 80, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 80, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 80, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 80, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 80, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 90, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 100, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 90, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 90, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 100, pos.y, pos.z + 40)
    );

    // X
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 120, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 120, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 120, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 120, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 130, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 140, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 140, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 140, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 140, pos.y, pos.z + 40)
    );

    // O
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 160, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 160, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 160, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 160, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 160, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 170, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 170, pos.y, pos.z + 40)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 180, pos.y, pos.z)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 180, pos.y, pos.z + 10)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 180, pos.y, pos.z + 20)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 180, pos.y, pos.z + 30)
    );
    DualObject(
        modelsURL + "road_center.obj",
        modelsURL + "road_center.mtl",
        new THREE.Vector3(pos.x + 180, pos.y, pos.z + 40)
    );
}